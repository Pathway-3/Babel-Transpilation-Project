"use strict";

const capitalLetters = function capitalLetters(str) {
  return str.toUpperCase();
};
const smallLetters = function smallLetters(str) {
  return str.toLowerCase();
};
module.exports = {
  capitalLetters,
  smallLetters
};